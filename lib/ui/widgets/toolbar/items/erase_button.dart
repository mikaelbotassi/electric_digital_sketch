import 'package:flutter/material.dart';

class EraseButton extends StatelessWidget {
  const EraseButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
